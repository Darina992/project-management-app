export type SignUpFormData = {
  name: string;
  login: string;
  password: string;
};

export type SignInFormData = {
  login: string;
  password: string;
};

export type AddTaskForm = {
  addTask: string;
  addDescription: string;
};
